<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_order_inherit_form" model="ir.ui.view">
        <field name="name">view.purchase.order.inherit.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet//group" position="after">
                <group>
                    <field name="revaluate" invisible="0"/>
                </group>
            </xpath>

            <xpath expr="//sheet//notebook//page[@name='products']//field[@name='analytic_distribution']" position="after">

                <field name="revaluate_asset_id" required="revaluate == True"/>
            </xpath>
        </field>
    </record>

</odoo>
