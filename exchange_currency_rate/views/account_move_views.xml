<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.view.form.inherit.exchange.currency.rate</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='journal_div']" position="replace">
                <div name="journal_div" class="d-flex" invisible="not is_exchange" groups="account.group_account_readonly,base.group_multi_currency">

                    <field name="journal_id" groups="account.group_account_readonly" options="{'no_create': True, 'no_open': True}" readonly="posted_before and name not in (False, '', '/')"/>

                    <span class="o_form_label mx-3 oe_edit_only" groups="account.group_account_readonly" invisible="move_type == 'entry'">
                        <span groups="base.group_multi_currency">in </span>
                    </span>

                    <div name="currency_div" groups="base.group_multi_currency" class="w-100" style="white-space: pre;" invisible="move_type == 'entry'">
                        <field name="currency_id" readonly="state != 'draft'" class="oe_inline" options="{'no_create': True}" context="{'search_default_active': 1, 'search_default_inactive': 1}"/>
                        <div class="break"/>
                        <div name="currency_conversion_div" class="d-flex text-muted float-start" invisible="currency_id == company_currency_id">

                            <span>1 </span>
                            <field name="currency_id" readonly="True" options="{'no_open': True}"/>
                            <span> = </span>

                            <!-- Show rate entered by user only if is_exchange -->
                            <field name="invoice_currency_rate" digits="[12,6]" readonly="True" invisible="not is_exchange"/>

                            <!-- Show system-computed rate only if is_exchange is False -->
                            <field name="invoice_company_currency_rate" digits="[12,6]" readonly="True" invisible="is_exchange"/>

                            <span></span>
                            <field name="company_currency_id" readonly="True" options="{'no_open': True}"/>
                        </div>
                    </div>
                </div>
                <div name="journal_div" invisible="is_exchange" class="d-flex" groups="account.group_account_readonly,base.group_multi_currency">
                    <field name="journal_id" groups="account.group_account_readonly" options="{'no_create': True, 'no_open': True}" readonly="posted_before and name not in (False, '', '/')"/>

                    <span class="o_form_label mx-3 oe_edit_only" groups="account.group_account_readonly" invisible="move_type == 'entry'">
                        <span groups="base.group_multi_currency">in </span>
                    </span>
                    <div name="currency_div" groups="base.group_multi_currency" class="w-100" style="white-space: pre;" invisible="move_type == 'entry'">
                        <field name="currency_id" readonly="state != 'draft'" class="oe_inline" options="{'no_create': True}" context="{'search_default_active': 1, 'search_default_inactive': 1}"/>
                        <div class="break"/>
                        <div name="currency_conversion_div" class="d-flex text-muted float-start" invisible="currency_id == company_currency_id">
                            <span>1 </span>
                            <field name="company_currency_id" readonly="True" options="{'no_open': True}"/>
                            <span> = </span>
                            <field name="invoice_currency_rate" digits="[12,6]" readonly="True"/>
                            <span></span>
                            <field name="currency_id" readonly="True" options="{'no_open': True}"/>
                        </div>
                    </div>
                </div>    
            </xpath>

            <xpath expr="//field[@name='payment_reference']" position="after">
                <field name="is_exchange" invisible="currency_id == company_currency_id" readonly="state != 'draft'" force_save="1"/>
                <field name="rate" digits="[12,6]" force_save="1" invisible="is_exchange == False" readonly="state != 'draft'"/>
            </xpath>
        </field>
    </record>
</odoo>
