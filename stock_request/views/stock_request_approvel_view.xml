<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="view_warehouse_form_inherit" model="ir.ui.view">
            <field name="name">stock.warehouse.form.inherit</field>
            <field name="model">stock.warehouse</field>
            <field name="inherit_id" ref="stock.view_warehouse"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='name']" position="after">
                    <group string="Warehouse Manager">
                        <field name="wh_manager"
                               widget="many2many_tags"
                               options="{'no_create': True, 'no_open': True}"
                        />
                    </group>
                </xpath>

                <xpath expr="//field[@name='wh_output_stock_loc_id']/..">
                    <field name="van_load_route_id" options="{'create':0}"/>
                    <field name="van_offload_route_id" options="{'create':0}"/>
                </xpath>
            </field>
        </record>

        <record id="view_stock_location_form_inherit" model="ir.ui.view">
            <field name="name">stock.location.form.inherit</field>
            <field name="model">stock.location</field>
            <field name="inherit_id" ref="stock.view_location_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='location_id']" position="after">
                    <group string="Location Responsible">
                        <field name="responsible_id"
                               widget="many2many_tags"
                               options="{'no_create': True, 'no_open': True}"
                               domain="[('id', 'in', stock_keeper_user_ids)]"
                        />
                        <field name="stock_keeper_user_ids" invisible="1"/> 
                        
                    </group>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
