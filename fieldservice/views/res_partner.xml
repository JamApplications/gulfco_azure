<odoo>
    <!-- Customers -->
    <record id="action_fsm_customer" model="ir.actions.act_window">
        <field name="name">Customers</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,list,form</field>
        <field
            name="context"
        >{'search_default_customer': 1, 'default_customer_rank': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a Customer.
            </p>
        </field>
    </record>


    <record id="fsm_gulfco_customer_partner_fields" model="ir.ui.view">
        <field name="name">res.partner.inherit.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="gulfco_customer_customized_data.res_partner_customer_detailed_data_inherit_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='vat_trn']" position="replace">
                <field name="vat_trn" required="contact_type == 'customer'"  invisible="fsm_person == True"/>
            </xpath>
            <xpath expr="//page[@name='detailed_customer_data']" position="attributes">
                <attribute name="invisible">fsm_person == True or contact_type not in ['customer', 'both']</attribute>
            </xpath>
        </field>
    </record>

    <record id="fsm_gulfco_vendor_partner_fields" model="ir.ui.view">
        <field name="name">res.partner.inherit.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="gulfco_vendor_customized_data.vendor_details_form_inherit_form"/>
        <field name="arch" type="xml">
<!--            <xpath expr="//page[@name='contact_type']" position="after">-->
<!--                <field name="vehicle_id"/>-->
<!--            </xpath>-->
            <xpath expr="//page[@name='vendor_details']" position="attributes">
                <attribute name="invisible">fsm_person == True or contact_type not in ['supplier', 'both']</attribute>
            </xpath>
        </field>
    </record>

    <record id="fsm_base_account_partner_fields" model="ir.ui.view">
        <field name="name">res.partner.account.inherit.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="account.view_partner_property_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='accounting']" position="attributes">
                <attribute name="invisible">fsm_person == True or not is_company and parent_id</attribute>
            </xpath>
        </field>
    </record>


     <record id="fsm_base_geolocalize_partner_fields" model="ir.ui.view">
        <field name="name">res.partner.geolocalize.inherit.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base_geolocalize.view_crm_partner_geo_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='geo_location']" position="attributes">
                <attribute name="invisible">fsm_person == True</attribute>
            </xpath>
        </field>
    </record>



    <!-- Field Service Fields -->
    <record id="fsm_partner_fields" model="ir.ui.view">
        <field name="name">res.partner.fsm.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">

            <xpath expr="//field[@name='child_ids']" position="replace">
                <field name="child_ids" mode="kanban"
                       domain="['|',('active', '=', True), ('active', '=', False)]"
                       context="{'default_parent_id': id, 'default_street': street,
                       'default_street2': street2, 'default_city': city, 'default_state_id': state_id,
                       'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang,  'active_test': False,
                       'default_user_id': user_id, 'default_type': 'other'}">
                                <kanban color="color">
                                    <field name="color"/>
                                    <field name="type"/>
                                    <field name="is_company"/>
                                     <field name="active"/>
                                    <templates>

                                        <t t-name="card" class="flex-row">
                                            <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>

                                            <aside class="o_kanban_aside_full">
                                                <field name="avatar_128" class="o_kanban_image_fill w-100" widget="image" options="{'img_class': 'object-fit-cover'}" alt="Contact image"/>
                                            </aside>
                                            <main class="ps-2 ps-md-0">
                                                <field name="name" class="fw-bold"/>
                                                <field name="function"/>
                                                <field name="email" widget="email"/>
                                                <div t-if="record.type.raw_value != 'contact'">
                                                    <div>
                                                        <field class="me-1" name="zip"/>
                                                        <field name="city"/>
                                                    </div>
                                                    <field class="me-1" name="state_id"/>
                                                    <field name="country_id"/>
                                                </div>
                                                <div t-if="record.phone.raw_value">Phone: <field name="phone"/></div>
                                                <div t-if="record.mobile.raw_value">Mobile: <field name="mobile"/></div>
                                            </main>
                                        </t>
                                    </templates>
                                </kanban>
                                <form string="Contact / Address">
                                    <sheet>
                                        <field name="type" required="1" widget="radio" options="{'horizontal': true}"/>
                                        <div class="text-muted oe_edit_only">
                                            <p class="mb-0" invisible="type != 'contact'">
                                                <span>Use this to organize the contact details of employees of a given company (e.g. CEO, CFO, ...).</span>
                                            </p>
                                            <p class="mb-0" invisible="type != 'invoice'">
                                                <span>Preferred address for all invoices. Selected by default when you invoice an order that belongs to this company.</span>
                                            </p>
                                            <p class="mb-0" invisible="type != 'delivery'">
                                                <span>Preferred address for all deliveries. Selected by default when you deliver an order that belongs to this company.</span>
                                            </p>
                                            <p class="mb-0" invisible="type != 'other'">
                                                <span>Other address for the company (e.g. subsidiary, ...)</span>
                                            </p>
                                        </div>
                                        <hr/>
                                        <group>
                                            <group>
                                                <field name="name" string="Contact Name" required="type in ['contact', 'fsm_location']" placeholder="e.g. New Address"/>
                                                <field name="title" options="{'no_open': True}" placeholder="e.g. Mr." invisible="type != 'contact'"/>
                                                <field name="function" placeholder="e.g. Sales Director" invisible="type != 'contact'"/>
                                                <label for="street" string="Address" invisible="type == 'contact'"/>
                                                <div invisible="type == 'contact'">
                                                    <div class="o_address_format" name="div_address">
                                                        <field name="street" placeholder="Street..." class="o_address_street"/>
                                                        <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                                                        <field name="city" placeholder="City" class="o_address_city"/>
                                                        <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                                                        <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                                                        <field name="country_id" placeholder="Country" class="o_address_country" options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}"/>
                                                    </div>
                                                </div>
                                            </group>
                                            <group>
                                                <field name="email" widget="email"/>
                                                <field name="phone" widget="phone"/>
                                                <field name="mobile" widget="phone"/>
                                                <field name="company_id" invisible="1"/>    <!-- Need to save value from parented record, cf onchange -->
                                            </group>
                                        </group>
                                        <group name="Attachments" invisible="type != 'fsm_location'">
                                            <field name="trade_license_file_name" invisible="1"/>
                                            <field name="trade_license_attachment" widget="binary"
                                                   filename="trade_license_file_name"
                                                   required="type == 'fsm_location' or contact_type in ['supplier', 'both']"/>


                                            <field name="vat_trn_file_name" invisible="1"/>
                                            <field name="vat_trn_attachment" widget="binary"
                                                   filename="vat_trn_file_name" required="type == 'fsm_location'"/>

                                        </group>
                                        <group>
                                            <field name="comment" placeholder="Internal notes..." nolabel="1"/>
                                        </group>
                                        <field name="lang" invisible="True"/>  <!-- Need to add lang to save default value from parented record -->
                                    </sheet>
                                </form>
                            </field>
            </xpath>


            <xpath expr="//field[@name='title']" position="replace">
                <field name="vehicle_id" string="Default Vehicle" invisible="fsm_person == False"/>
                <field name="working_schedule_id" string="Working Schedule" invisible="fsm_person == False"/>
<!--                <field name="title" options="{'no_open': True}" placeholder="e.g. Mister" invisible="fsm_person == True"/>-->
            </xpath>
            <xpath expr="//field[@name='category_id']" position="replace">

                <field name="category_id" widget="many2many_tags" options="{'color_field': 'color'}" invisible="fsm_person == True"/>
            </xpath>
            <xpath expr="//page[@name='contact_addresses']" position="attributes">
                <attribute name="invisible">fsm_person == True</attribute>
            </xpath>
            <xpath expr="//page[@name='sales_purchases']" position="attributes">
                <attribute name="invisible">fsm_person == True</attribute>
            </xpath>
            <xpath expr="//page[@name='internal_notes']" position="attributes">
                <attribute name="invisible">fsm_person == True</attribute>
            </xpath>

            <xpath expr="//field[@name='company_type']" position="replace">
                 <field name="company_type" widget="radio" options="{'horizontal': true}" invisible="fsm_person == True"/>
            </xpath>
            <div name="button_box" position="inside">
                <button
                    class="oe_stat_button"
                    type="object"
                    invisible="owned_location_count == 0"
                    name="action_open_owned_locations"
                    icon="fa-map-marker"
                    context="{'default_owner_id': id}"
                    groups="fieldservice.group_fsm_user"
                >
                    <field
                        string="Owned Locations"
                        name="owned_location_count"
                        widget="statinfo"
                    />
                </button>
            </div>
            <notebook position="inside">
                <page string="Field Service" invisible="contact_type not in ('worker')">
<!--                    groups="fieldservice.group_fsm_user_own">-->
                    <group>
                        <group>
                            <field name="fsm_location" readonly="0" />
                            <field name="fsm_person" readonly="0" />
                            <field name="van_location" invisible="not fsm_person" />
                            <field name="internal_user"/>
                            <field
                                name="service_location_id"
                                context="{'default_contact_id': id}"
                            />
                        </group>
                        <group />
                    </group>
                </page>
            </notebook>
        </field>
    </record>
    <record id="res_partner_search_reference" model="ir.ui.view">
        <field name="name">res.partner.search.reference</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter" />
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="ref" />
            </field>
        </field>
    </record>
</odoo>
